name: Cron Alerta Eventos

on:
  schedule:
    - cron: "55 7 * * *"
  workflow_dispatch:
jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Acordar o Render
        run: curl -s https://chatbot-6viu.onrender.com/live
