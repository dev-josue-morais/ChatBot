name: keepAlive

on:
  schedule:
    - cron: "10-59/20 0-1,8-23 * * *"
  workflow_dispatch:

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Acordar o Render
        run: curl -s https://chatbot-6viu.onrender.com/live
